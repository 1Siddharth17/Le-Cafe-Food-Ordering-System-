# Le-Cafe-Food-Ordering-System-
The Le Cafe' project is a web-based Food Ordering System built using PHP and MySQL, styled with Bootstrap. It facilitates a platform for a restaurant to manage its food items and allow customers to place orders. The system implements user authentication for both Managers and Customer(users) and provides functionality for managers to add food items.
